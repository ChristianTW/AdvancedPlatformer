using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;
using SuperPupSystems.Helper;

namespace SuperPupSystems.Helper
{

public class AttackArea : PlayerHealth
{

    /*
    private int damage = 50;

    private void OnTriggerEnter2D(Collider2D collider)
    {

        if(collider.GetComponent<currentHealth>() != null)
        {
            currentHealth health = collider.GetComponent<currentHealth>();
            health.Damage(damage);
        }
    }
    */
    
    

    /*
    private void OnTriggerEnter2D(Collider2D collider)
    {
        if(collider.GetComponent<isAlive>() == true) 
        {
            isAlive = false;
        }
    }
    */
    


}
}
